# ──────────────────────────────────────────────────────────────────────────────
# Hugo base
# ──────────────────────────────────────────────────────────────────────────────
baseurl                = "https://blog.usfkhoury.com/"
languageCode           = "en-us"
defaultContentLanguage = "en"
# Set to true if your default language uses CJK characters
hasCJKLanguage         = false

title = "Youssef El Khoury"
theme = "hugo-theme-stack"

enableRobotsTXT = true

[pagination]
    pagerSize = 5

# Permalink format for blog posts and standalone pages
# Changing these will break existing URLs — leave as-is unless you redirect old URLs
[permalinks]
    blog = "/blog/:slug/"
    page = "/:slug/"

# ──────────────────────────────────────────────────────────────────────────────
# Markup (goldmark renderer + syntax highlighting + ToC)
# ──────────────────────────────────────────────────────────────────────────────
[markup]
    [markup.goldmark]
        [markup.goldmark.extensions]
            # Math passthrough — lets LaTeX blocks pass through to MathJax/KaTeX
            [markup.goldmark.extensions.passthrough]
                enable = true
                [markup.goldmark.extensions.passthrough.delimiters]
                    block  = [["\\[", "\\]"], ["$$", "$$"]]
                    inline = [["\\(", "\\)"], ["$", "$"]]

        [markup.goldmark.renderer]
            # Allow raw HTML inside Markdown (needed for shortcodes that emit HTML)
            unsafe = true

    [markup.tableOfContents]
        startLevel = 1
        endLevel   = 4
        ordered    = true

    [markup.highlight]
        noClasses          = false
        codeFences         = true
        guessSyntax        = true
        lineNos            = true
        lineNumbersInTable = true
        lineNoStart        = 1
        tabWidth           = 4

# ──────────────────────────────────────────────────────────────────────────────
# Related posts
# ──────────────────────────────────────────────────────────────────────────────
[related]
    includeNewer = true
    threshold    = 60
    toLower      = false
    indices      = [
        { name = "tags",       weight = 100 },
        { name = "categories", weight = 200 },
    ]

# ──────────────────────────────────────────────────────────────────────────────
# Theme params
# ──────────────────────────────────────────────────────────────────────────────
[params]
    # Sections rendered on the homepage and in the RSS feed
    mainSections   = ["blog"]
    rssFullContent = true
    # "default" = Hugo's default sort order | "lastmod" = sort by last modified date
    SortBy         = "default"
    # Path to favicon — relative to assets/ folder
    favicon        = ""

[params.footer]
    since      = 2020
    customText = ""

[params.dateFormat]
    # Hugo date format tokens — see https://gohugo.io/functions/time/format/
    published   = "Jan 02, 2006"
    lastUpdated = "Jan 02, 2006 15:04 MST"

[params.sidebar]
    subtitle = "My personal blog, exploring and documenting the Lebanese tradition of doing things yourself"
    compact  = false
    # Emoji displayed next to the avatar
    emoji    = ""
    # Avatar: full URL (remote) or assets/-relative path (local, e.g. "img/avatar.png")
    avatar   = "https://blog.usfkhoury.com/images/main/logo.jpg"

[params.article]
    headingAnchor = false
    math          = true
    toc           = true
    readingTime   = true

    [params.article.list]
        # Show tags below each article card in list views
        showTags = true

    [params.article.license]
        enabled = false
        default = "Licensed under [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/)"

    # Native mermaid diagram support — diagrams render from ```mermaid code blocks.
    # Auto-switches theme with site light/dark mode.
    [params.article.mermaid]
        look                  = "classic"       # classic | handDrawn
        lightTheme            = "default"       # default | neutral | dark | forest | base | null
        darkTheme             = "dark"          # default | neutral | dark | forest | base | null
        securityLevel         = "loose"         # strict | loose | antiscript | sandbox
        htmlLabels            = true
        transparentBackground = false
        # lightThemeVariables = { primaryColor = "#ff0000" }
        # darkThemeVariables  = { primaryColor = "#00ff00" }

    # Emoji icons shown in GitHub-style alert blocks (> [!NOTE] etc.)
    [params.article.alertIcon]
        note      = "📝"
        tip       = "💡"
        important = "📌"
        warning   = "⚠️"
        caution   = "🚨"

# Sidebar widgets — homepage and per-page
[params.widgets]
    homepage = [
        { type = "search" },
        { type = "categories", params = { limit = 10 } },
        { type = "tag-cloud",  params = { limit = 10 } },
    ]
    page = [
        { type = "toc" },
    ]

# Open Graph / Twitter card
[params.opengraph.twitter]
    site = ""                   # your Twitter/X handle, e.g. "@usfkhoury"
    card = "summary_large_image"

# Light / dark colour scheme toggle
[params.colorScheme]
    toggle  = true
    default = "auto"   # auto | light | dark

# Image processing
[params.imageProcessing]
    autoOrient = false

    [params.imageProcessing.external]
        timeout = "5s"

    [params.imageProcessing.content]
        enabled = true
        widths  = [800, 1600, 2400]

    [params.imageProcessing.thumbnail]
        enabled = true

# GDPR cookie consent banner
[params.cookies]
    enabled      = false
    showSettings = true

    [params.cookies.categories]
        analytics  = true
        functional = true

# ── Comments ──────────────────────────────────────────────────────────────────
# Disabled. To enable: set enabled = true, set provider (disqus, utterances,
# giscus, …) and add that provider's settings here — full list of providers
# and their options: https://stack.jimmycai.com/config/comments
[params.comments]
    enabled  = false
    provider = ""

# ──────────────────────────────────────────────────────────────────────────────
# Navigation menu
# ──────────────────────────────────────────────────────────────────────────────
[[menu.main]]
    identifier = "home"
    name       = "Home"
    url        = "/"
    weight     = 1
    [menu.main.params]
        icon = "home"

[[menu.main]]
    identifier = "search"
    name       = "Search"
    url        = "/search/"
    weight     = 3
    [menu.main.params]
        icon = "search"

# ──────────────────────────────────────────────────────────────────────────────
# Social links (sidebar)
# ──────────────────────────────────────────────────────────────────────────────
[[menu.social]]
    identifier = "github"
    name       = "GitHub"
    url        = "https://github.com/usfkhoury"
    weight     = 1
    [menu.social.params]
        icon = "brand-github"

[[menu.social]]
    identifier = "instagram"
    name       = "Instagram"
    url        = "https://www.instagram.com/usfkhoury"
    weight     = 2
    [menu.social.params]
        icon = "brand-instagram"

[[menu.social]]
    identifier = "linkedin"
    name       = "LinkedIn"
    url        = "https://www.linkedin.com/in/youssef-el-khoury-124648155"
    weight     = 3
    [menu.social.params]
        icon = "brand-linkedin"

[[menu.social]]
    identifier = "telegram"
    name       = "Telegram"
    url        = "https://t.me/usfkhoury"
    weight     = 4
    [menu.social.params]
        icon = "brand-telegram"

[[menu.social]]
    identifier = "email"
    name       = "Email"
    url        = "mailto:usfkhouryy@gmail.com"
    weight     = 5
    [menu.social.params]
        icon = "mail"
